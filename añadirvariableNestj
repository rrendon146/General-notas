añadir variable en el nestj:

en caso de que salga error en la consola web de Uncaught typerror cannot read property "variable no declarada u otras razones"


const newArea = await this.areaModel.create( createAreadto );
return this.areaModel.save( newArea );
let data = await this.areaModel.save( newArea );
return {
  data : data,
  version : '1'
 };
}


O TAMBIEN POR LAS COLUMNAS TIENEN QUE ESTAR CON EL NUMERO DE ACORDE AL HOME.PHP CON HOME.JS "TR"
